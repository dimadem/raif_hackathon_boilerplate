services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - model_cache:/app/model_cache
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

volumes:
  model_cache:
